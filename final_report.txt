% !TEX program = pdflatex
\documentclass[11pt]{article}
\usepackage{acl2023}
\usepackage{times}
\usepackage{latexsym}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xspace}
\usepackage{color}

\newcommand{\dlogp}{\Delta_{\mathrm{logprob}}}
\newcommand{\dfact}{\Delta_{\mathrm{fact}}}
\newcommand{\dltwo}{\Delta_{L2}}
\newcommand{\lodo}{\textsc{Lodo}\xspace}
\newcommand{\cdr}{\textsc{Cdr}\xspace}
\newcommand{\thr}{\tau}

\title{Influence Is Not Usefulness: \\
Factuality-Aware Document Attribution in Retrieval-Augmented Generation}

\author{
  Gawon Lim \\
  University of Illinois Urbana-Champaign \\
  \texttt{gawonl2@illinois.edu}
}

\begin{document}
\maketitle

%% ============================================================
\begin{abstract}
%% ============================================================

Existing attribution methods for Retrieval-Augmented Generation (RAG) identify
document importance through causal influence on model outputs---typically
measured by log-probability degradation after document removal. We show that
this definition conflates two distinct properties: a document can strongly
influence model confidence while providing no factual benefit, or even pushing
the model toward an incorrect answer. We propose \lodo (Leave-One-Document-Out),
a three-axis evaluation framework that jointly measures factual degradation,
log-probability collapse, and layer-wise representation drift when each retrieved
document is removed. To directly test attribution under adversarial conditions,
we construct a \emph{mixed retrieval} setting from the RGB benchmark: each
query receives one ground-truth document from \texttt{en\_mid} alongside
$(n-1)$ counter-factual documents from \texttt{en\_counter\_mid}, ensuring
the model can produce a correct baseline while the majority of evidence is
misleading. Applying \lodo to this setting with Llama-3.1-8B-Instruct, we
find a Collapse Divergence Rate (\cdr) of \textbf{63.2\%} among logprob-important
documents overall---but with a striking asymmetry: ground-truth documents are
factuality-critical in 62.5\% of cases, while counter-factual documents are
confidence-only in 71.2\% of cases. A factuality-aware composite score achieves
P@1\,=\,1.00 and MRR\,=\,1.00 across 12 non-trivial queries, versus 0.73 and
0.83 for logprob-only ranking. A mechanistic analysis reveals an \emph{Integration
Horizon} at layers 18--20, and shows that factuality-critical and confidence-only
documents produce indistinguishable layer-wise drift profiles---confirming that
representation drift, like logprob collapse, cannot alone identify factually
useful documents.

\end{abstract}


%% ============================================================
\section{Introduction}
%% ============================================================

Retrieval-Augmented Generation (RAG) grounds language model outputs in
externally retrieved documents, improving factual accuracy in open-domain
question answering and knowledge-intensive tasks~\cite{lewis2020rag}.
A fundamental question for both reliability and interpretability is:
\emph{which of the retrieved documents actually caused the model to produce
the correct answer?}

Existing attribution methods answer this by measuring causal influence.
ContextCite~\cite{cohen2024contextcite} trains a sparse linear surrogate over
context ablations. TracLLM~\cite{wang2025tracllm} applies recursive partitioning
to long contexts. AttriBoT~\cite{dubey2025attribot} approximates leave-one-out
attribution via cached activations. All three methods score a document as
important when its removal changes the model's output distribution---typically
quantified by a drop in log-likelihood of the generated response.

\paragraph{The gap.}
Causal influence on output probability is not the same as factual impact.
A document that confidently asserts a false fact can cause large log-probability
collapse when removed, because the model has ``anchored'' its generation on that
misinformation---yet the correct answer survives unchanged. Conversely, a document
can be necessary for the correct answer without making a large dent in aggregate
log-probability. Attribution methods that rely solely on likelihood degradation
cannot distinguish these cases: they measure \emph{what shifted the model's
output}, not \emph{whether that shift affected factual correctness}.

This distinction is not academic. In a medical or legal RAG system, a misleading
document that scores as ``highly important'' under influence-based attribution
provides genuinely wrong guidance: it suggests the document should be trusted,
when in fact it is the document the system should resist.

\paragraph{This work.}
We propose \textbf{Factuality-Aware \lodo (Leave-One-Document-Out)}, a
three-axis evaluation framework that simultaneously measures:
(i)~\emph{factual degradation} ($\dfact$)---whether removing a document changes
the model's factual correctness;
(ii)~\emph{log-probability collapse} ($\dlogp$)---how much the model's
confidence in its original answer drops; and
(iii)~\emph{representation drift} ($\dltwo$)---how much internal hidden states
shift layer by layer.

To test attribution in a controlled but realistic adversarial setting, we
construct a \emph{mixed retrieval} scenario: each query is provided with exactly
one ground-truth document (from the RGB \texttt{en\_mid} subset) and
$(n-1)$ counter-factual documents (from \texttt{en\_counter\_mid}). This design
ensures the model has access to the correct answer while the majority of the
context actively misleads it---a realistic proxy for retrieval noise in
real-world RAG systems.

\paragraph{Contributions.} We make the following contributions:
\begin{enumerate}[noitemsep,leftmargin=*]
  \item We formalize the distinction between causal influence and factual
        impact in document attribution, and show the two are empirically
        misaligned even when the model can produce a correct baseline.
  \item We propose \lodo, a three-axis post-hoc evaluation framework that
        jointly measures factual degradation, likelihood degradation, and
        representation drift per removed document.
  \item We quantify collapse divergence in a mixed retrieval setting: CDR
        = 63.2\% overall, with ground-truth documents being factuality-critical
        62.5\% of the time and counter-factual documents being confidence-only
        71.2\% of the time.
  \item We introduce a six-category importance taxonomy and show that
        factuality-aware ranking achieves P@1\,=\,1.00 versus 0.73 for
        logprob-only ranking across 12 non-trivial queries.
  \item We identify an \emph{Integration Horizon} at transformer layers 18--20
        and show that factuality-critical and confidence-only documents produce
        indistinguishable representation drift, confirming that $\dfact$ is
        the irreducible axis of factuality-aware attribution.
\end{enumerate}


%% ============================================================
\section{Related Work}
%% ============================================================

\subsection{Document Attribution in RAG}

Attribution methods for RAG estimate which context segments contributed to a
specific model output. ContextCite~\cite{cohen2024contextcite} fits a sparse
linear model via LASSO over context-ablation outputs to score each sentence.
TracLLM~\cite{wang2025tracllm} handles long contexts through recursive
partitioning and contribution-score denoising, enabling attribution over
multi-document contexts. AttriBoT~\cite{dubey2025attribot} approximates
leave-one-out attribution efficiently using cached activations and proxy
models, enabling practical attribution at scale.

A shared assumption across these methods is that causal contribution to the
output is the right proxy for document importance. A document is scored as
important if its absence measurably changes the model's output distribution.
This conflates influence with factual impact: a document asserting a confidently
wrong answer can score highly important under all three methods.

\subsection{Mechanistic Interpretability of RAG Evidence}

Prior mechanistic work has examined how transformer models process in-context
information. Attention patterns in early layers encode structural dependencies,
while mid-to-late layers integrate semantic content~\cite{meng2022rome,
geva2021transformer}. In RAG settings, evidence shows that factual knowledge
is stored in and retrieved from mid-layer MLP
modules~\cite{meng2022rome,dai2022knowledge}, but the interaction between
parametric knowledge and retrieved context at the representation level is
less studied. Our layer-wise drift analysis extends this direction to
the specific question of \emph{where} retrieved documents influence generation.

\subsection{Factuality in RAG Evaluation}

Factuality evaluation for RAG has largely focused on end-to-end
metrics~\cite{chen2024rgb,es2023ragas}. The RGB benchmark~\cite{chen2024rgb}
evaluates noise robustness, rejection behavior, and counterfactual resistance
across retrieval conditions. Our work constructs a mixed retrieval scenario from
RGB's standard (\texttt{en\_mid}) and counterfactual (\texttt{en\_counter\_mid})
subsets, placing factual correctness in direct tension with retrieved evidence.


%% ============================================================
\section{Problem Formulation}
%% ============================================================

\paragraph{Setup.}
Let $q$ be a query and $\mathbf{D} = \{d_1, \dots, d_n\}$ a set of $n$
retrieved documents. A model $\mathcal{M}$ generates a response
$\hat{y} = \mathcal{M}(q, \mathbf{D})$, and a factuality scorer $f$
assigns a binary correctness label $f(\hat{y}, y^*) \in \{0, 1\}$,
where $y^*$ is the ground-truth answer.

\paragraph{LODO ablation.}
For each document $d_i \in \mathbf{D}$, we define the \emph{ablated context}
$\mathbf{D}_{-i} = \mathbf{D} \setminus \{d_i\}$. We generate a new response
$\hat{y}_{-i} = \mathcal{M}(q, \mathbf{D}_{-i})$ and evaluate three axes of
degradation relative to the full-context \emph{baseline}:

\begin{align}
  \dfact(d_i) &= f(\hat{y}, y^*) - f(\hat{y}_{-i}, y^*) \label{eq:dfact}\\
  \dlogp(d_i) &= \log P_{\mathcal{M}}(\hat{y} \mid q, \mathbf{D}_{-i})
                 - \log P_{\mathcal{M}}(\hat{y} \mid q, \mathbf{D}) \label{eq:dlogp}\\
  \dltwo^{(\ell)}(d_i) &=
    \bigl\lVert \bar{h}^{(\ell)}(\mathbf{D}) -
    \bar{h}^{(\ell)}(\mathbf{D}_{-i}) \bigr\rVert_2 \label{eq:dl2}
\end{align}

where $\bar{h}^{(\ell)}$ denotes the mean hidden state at transformer layer
$\ell$ over all output tokens. $\dlogp$ is computed via teacher forcing over
the fixed baseline answer $\hat{y}$, making it fully deterministic and
independent of sampling temperature.

\paragraph{Causal influence vs factual impact.}
Prior attribution methods define document importance as causal influence:
document $d_i$ is important if $|\dlogp(d_i)|$ exceeds a threshold $\thr$.
We argue this definition is incomplete. We instead define:

\begin{definition}[Factuality-Impacting]
  $d_i$ is \emph{factuality-impacting} if $\dfact(d_i) \neq 0$: its removal
  changes the model's factual correctness in either direction.
\end{definition}

\begin{definition}[Logprob-Important]
  $d_i$ is \emph{logprob-important} if $\dlogp(d_i) < -2.0$: its removal
  causes strong log-probability collapse in the original answer.
\end{definition}

A document can be logprob-important without being factuality-impacting (collapse
divergence), and can be factuality-impacting with only a modest logprob drop
(fact-only). The Collapse Divergence Rate formalizes how often
the two concepts disagree:

\begin{equation}
  \cdr = \frac{|\{d_i : \dlogp < -\thr,\ \dfact = 0\}|}
              {|\{d_i : \dlogp < -\thr\}|}
  \label{eq:cdr}
\end{equation}

\paragraph{Factuality-aware taxonomy.}
We classify each ablated document into one of six mutually exclusive categories
based on its ($\dlogp$, $\dfact$) pair:

\begin{center}
\small
\begin{tabular}{lll}
\toprule
Category & $\dlogp$ & $\dfact$ \\
\midrule
Factuality-critical   & $< -\thr$ & $> 0$ \\
Confidence-only       & $< -\thr$ & $= 0$ \\
Factuality-disrupting & $< -\thr$ & $< 0$ \\
Fact-only             & $\geq -\thr$ & $> 0$ \\
Neutral               & $\geq -\thr$ & $= 0$ \\
Factuality-weak       & $\geq -\thr$ & $< 0$ \\
\bottomrule
\end{tabular}
\end{center}

\emph{Factuality-critical} documents are both causally influential and
factually necessary. \emph{Factuality-disrupting} documents are influential
and actively push the model toward incorrect answers.
\emph{Confidence-only} documents are the CDR population:
influential by the likelihood criterion but factually irrelevant.


%% ============================================================
\section{Method: Factuality-Aware LODO}
%% ============================================================

\subsection{LODO Procedure}

For each query $q$ with retrieved documents $\mathbf{D}$:
\begin{enumerate}[noitemsep,leftmargin=*]
  \item Generate the baseline answer $\hat{y}$ from $\mathcal{M}(q, \mathbf{D})$
        and record its factuality score $f(\hat{y}, y^*)$ and log-probability.
  \item For each document $d_i \in \mathbf{D}$:
        \begin{enumerate}[noitemsep]
          \item Generate a new answer $\hat{y}_{-i}$ from $\mathcal{M}(q,
                \mathbf{D}_{-i})$ and compute $\dfact(d_i)$.
          \item Compute $\dlogp(d_i)$ via teacher-forcing $\hat{y}$ under the
                ablated context (deterministic; no sampling required).
          \item Compute $\dltwo^{(\ell)}(d_i)$ at selected layers $\ell$.
        \end{enumerate}
  \item Classify each $d_i$ into the factuality-aware taxonomy using
        threshold $\thr = 2.0$.
\end{enumerate}

\subsection{Factuality-Aware Importance Score}

In addition to the binary taxonomy, we define a \emph{factuality-aware causal
score} for continuous ranking:
\begin{equation}
  s_{\mathrm{FA}}(d_i) = \max(0,\,-\dlogp(d_i)) \cdot \max(0,\,\dfact(d_i))
  \label{eq:fa_score}
\end{equation}

This score is non-zero only when a document is both logprob-important
\emph{and} factuality-critical, directly operationalizing our definition of
document importance.

\subsection{Layer-Wise and Token-Wise Analysis}

\paragraph{Integration Horizon.}
We track $\dltwo^{(\ell)}$ across all transformer layers. Prior work
suggests factual grounding occurs in mid-to-late layers; we test whether
documents classified as factuality-critical vs confidence-only produce
distinguishable layer-wise drift profiles.

\paragraph{Fact-token logprob.}
For each output token $t_j$, we compute the per-token logprob drop
$\delta_j = \log p(t_j | t_{<j}, \mathbf{D}_{-i}) - \log p(t_j | t_{<j},
\mathbf{D})$. We classify tokens as \emph{factual} (digits, dates, capitalized
proper-noun tokens) or \emph{structural} (prepositions, function words) and
compare the concentration of logprob collapse across token types.


%% ============================================================
\section{Experimental Setup}
%% ============================================================

\paragraph{Mixed retrieval setting.}
We construct a controlled adversarial retrieval scenario from two subsets of
the RGB benchmark~\cite{chen2024rgb}: \textbf{\texttt{en\_mid}} (standard
retrieval; documents support the correct answer) and \textbf{\texttt{en\_counter\_mid}}
(counterfactual retrieval; documents are modified versions of the same passages
but assert a wrong answer). Both subsets share the same 50 query IDs.

For each query and each passage count $n \in \{3, 5, 7, 10\}$, we construct
a mixed context:
\begin{itemize}[noitemsep,leftmargin=*]
  \item \textbf{1 ground-truth (GT) document}: sampled from the \texttt{positive}
        field of \texttt{en\_mid} for that query.
  \item \textbf{$(n-1)$ counter-factual (CF) documents}: sampled from the
        \texttt{positive} field of \texttt{en\_counter\_mid} for that query.
\end{itemize}
Documents are shuffled before presentation. This design guarantees the model
has access to the correct answer (enabling a non-trivial baseline) while the
majority of the context actively misleads it. The \texttt{positive} field in
\texttt{en\_counter\_mid} contains documents that are topically relevant but
assert a factually incorrect answer (e.g., with modified dates or wrong entity
names); the \texttt{negative} field contains irrelevant/noisy documents and
is excluded from all experiments.

\paragraph{Why this setting?}
A purely counter-factual context (all CF documents, no GT) produces baseline
accuracy of 0--10\%: factuality-critical cases ($\dfact > 0$) are structurally
impossible when the baseline is already wrong. The mixed setting corrects this:
baseline accuracy rises to 20--30\% depending on passage count, making the
full taxonomy observable. Crucially, it also mirrors a realistic retrieval
failure mode---a retriever that retrieves mostly noisy or adversarial documents
alongside a single relevant one.

\paragraph{Model.}
All experiments use \textbf{Llama-3.1-8B-Instruct} ($\tau = 0.7$ for
generation; teacher-forcing for $\dlogp$ and $\dltwo$).

\paragraph{Scale.}
We run \lodo on 10 queries from the shared query pool, using random seed 42
for document sampling and shuffling. The sweep produces \textbf{198 ablations}
across all passage counts ($30 + 44 + 55 + 69$ for $n \in \{3, 5, 7, 10\}$).
We also retain 275 ablations from \texttt{en\_refine} in the combined analysis
table (473 rows total), but passage-sweep statistics are reported on the 198
sweep ablations unless otherwise noted.


%% ============================================================
\section{Results}
%% ============================================================

We organize results around five research questions.

\subsection{RQ1: Does Likelihood-Based Importance Overestimate Factual Impact?}

\paragraph{Collapse Divergence Rate.}
Across the 198 sweep ablations, 68 documents (34.3\%) satisfy the
logprob-importance criterion ($\dlogp < -2.0$). Among these, \textbf{43
(63.2\%) cause zero factual change}---classified as confidence-only. 16 are
factuality-critical (23.5\%; FCR) and 9 are factuality-disrupting (13.2\%;
FDCR). The overall \cdr = 63.2\% (Table~\ref{tab:cdr}).

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Group & $N_{\mathrm{imp}}$ & CDR & FCR & FDCR \\
\midrule
All ablations            & 71 & 64.8\% & 22.5\% & 12.7\% \\
Sweep only               & 68 & 63.2\% & 23.5\% & 13.2\% \\
\midrule
$n=3$                    & 21 & 57.1\% & 23.8\% & 19.1\% \\
$n=5$                    & 17 & 64.7\% & 23.5\% & 11.8\% \\
$n=7$                    & 17 & 58.8\% & 23.5\% & 17.6\% \\
$n=10$                   & 13 & 76.9\% & 23.1\% & 0.0\% \\
\midrule
GT documents             & 16 & 37.5\% & 62.5\% & 0.0\% \\
CF documents             & 52 & 71.2\% & 11.5\% & 17.3\% \\
\bottomrule
\end{tabular}
\caption{Collapse Divergence Rate (CDR), Factuality-Critical Rate (FCR), and
Factuality-Disrupting Rate (FDCR) at $\Delta_{\text{logprob}} < \tau = 2.0$.
The doc-type rows show the striking asymmetry: ground-truth documents are
factuality-critical 62.5\% of the time, while counter-factual documents are
confidence-only 71.2\% of the time.}
\label{tab:cdr}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E1_cdr_by_passage_num.png}
  \caption{CDR (orange), FCR (blue), and FDCR (green) as fractions of
           logprob-important documents, by passage count. CDR ranges from
           57\% to 77\%, declining at lower $n$ where each document carries
           more weight.}
  \label{fig:cdr_by_pn}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E1_cdr_by_doc_type.png}
  \caption{CDR and FCR split by document type (ground-truth vs counter-factual).
           The contrast is striking: GT documents are predominantly factuality-critical
           while CF documents are predominantly confidence-only.}
  \label{fig:cdr_by_dt}
\end{figure}

\paragraph{Finding.}
Despite having access to one ground-truth document, most logprob-important
documents in the mixed setting are still confidence-only. The doc-type breakdown
reveals the attribution challenge: logprob-only methods cannot distinguish the
62.5\%-FCR ground-truth documents from the 71.2\%-CDR counter-factual ones
using $\dlogp$ alone.

\subsection{RQ2: Can Factuality-Aware Scoring Separate Documents by Factuality Impact?}

\paragraph{Importance taxonomy.}
Across all 473 ablations (sweep + en\_refine), document importance decomposes
as shown in Figure~\ref{fig:taxonomy_stacked}: 79.9\% are \emph{neutral},
9.7\% are \emph{confidence-only}, 3.6\% are \emph{factuality-weak}, 3.4\%
are \emph{factuality-critical}, 1.9\% are \emph{factuality-disrupting}, and
1.5\% are \emph{fact-only} (Table~\ref{tab:taxonomy}).

\begin{table}[h]
\centering
\small
\begin{tabular}{lrr}
\toprule
Category & Count & \% \\
\midrule
Neutral               & 378 & 79.9\% \\
Confidence-only       &  46 &  9.7\% \\
Factuality-weak       &  17 &  3.6\% \\
Factuality-critical   &  16 &  3.4\% \\
Factuality-disrupting &   9 &  1.9\% \\
Fact-only             &   7 &  1.5\% \\
\bottomrule
\end{tabular}
\caption{Factuality-aware importance taxonomy across 473 ablations.
Factuality-critical cases now constitute a meaningful 3.4\% of all ablations,
driven by the presence of ground-truth documents in the mixed retrieval setting.}
\label{tab:taxonomy}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E2_taxonomy_stacked_by_passage_num.png}
  \caption{Importance taxonomy by passage count. Factuality-critical documents
           (blue) appear consistently at every $n$, driven by the ground-truth
           document in each query. Confidence-only documents (orange) remain the
           dominant non-neutral category from counter-factual documents.}
  \label{fig:taxonomy_stacked}
\end{figure}

\paragraph{Ranking evaluation.}
Among 40 total query-passage pairs, 12 contain at least one factuality-impacting
document (Table~\ref{tab:ranking}). \textbf{Factuality-aware scoring
(Eq.~\ref{eq:fa_score}) achieves P@1\,=\,1.00 and MRR\,=\,1.00} across all
12 non-trivial queries, while logprob-only scoring achieves P@1\,=\,0.73 and
MRR\,=\,0.83. The Spearman correlation with $\dfact$ is 0.64 for
factuality-aware scoring versus 0.06 for logprob-only.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
Method & P@1 & MRR & Spearman $r$ \\
\midrule
Logprob-only         & 0.73 & 0.83 & 0.06 \\
Factuality-aware     & \textbf{1.00} & \textbf{1.00} & \textbf{0.64} \\
Harmfulness-aware    & 0.46 & 0.65 & $-$0.63 \\
\bottomrule
\end{tabular}
\caption{Ranking evaluation on 12 non-trivial queries ($n_q = 12$) with at
least one factuality-impacting document. Factuality-aware scoring correctly
identifies the factuality-impacting document at rank 1 in all 12 queries.
Logprob-only fails on 3 of 12 (P@1\,=\,0.73) by placing a confidence-only
counter-factual document above the ground-truth one.}
\label{tab:ranking}
\end{table}

\paragraph{Why logprob-only fails.}
In the 3 queries where logprob-only ranks incorrectly, the top-ranked document
is a counter-factual document with a larger $|\dlogp|$ than the ground-truth
document---a classic collapse divergence case. The GT document causes a smaller
logprob collapse but is the sole determinant of factual correctness.

\subsection{RQ3: How Does Passage Count Affect the Influence-Usefulness Gap?}

Table~\ref{tab:sweep} and Figure~\ref{fig:gap} present the passage-number sweep.

\begin{table}[h]
\centering
\small
\begin{tabular}{rrrrrrrr}
\toprule
$n$ & abl. & base acc & logprob-imp & fact-impact & gap & CDR \\
\midrule
3  & 30 & 30\%  & 70.0\% & 16.7\% & 53.3\% & 57.1\% \\
5  & 44 & 30\%  & 38.6\% & 11.4\% & 27.3\% & 64.7\% \\
7  & 55 & 20\%  & 30.9\% &  7.3\% & 23.6\% & 58.8\% \\
10 & 69 & 30\%  & 18.8\% & 11.6\% &  7.2\% & 76.9\% \\
\bottomrule
\end{tabular}
\caption{Influence-usefulness gap by passage count in the mixed retrieval setting.
Baseline accuracy is 20--30\% at all $n$ (vs.\ 0--10\% in the pure counterfactual
setting). The influence-usefulness gap narrows as $n$ increases because more
CF documents dilute each document's logprob impact.}
\label{tab:sweep}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E5_influence_usefulness_gap_by_passage.png}
  \caption{Logprob-important rate (orange), fact-impact rate (blue), and the
           gap between them (shaded) alongside baseline accuracy (black dashed).
           The gap narrows with $n$ as the single GT document is diluted by
           more CF documents, reducing each document's individual logprob impact.}
  \label{fig:gap}
\end{figure}

\paragraph{The dilution effect.}
As $n$ increases, the logprob-important rate drops sharply (70\% at $n=3$ to
18.8\% at $n=10$) while the fact-impact rate declines more slowly (16.7\% to
11.6\%). The CDR increases with $n$, reflecting that the single GT document's
share of logprob collapse diminishes relative to the pool of CF documents. At
$n=10$, the GT document produces a smaller logprob drop because 9 competing
CF documents collectively anchor the model's output distribution.

\subsection{RQ4: Where Inside the Model Does Evidence Integration Occur?}

\paragraph{Integration Horizon.}
Layer-wise drift trajectories from detailed case studies (10 ablations with
full 33-layer coverage) reveal a consistent structural pattern
(Figure~\ref{fig:layerwise}): drift is near zero through layers 0--17,
then rises sharply at layers 18--20. We term this the
\textbf{Integration Horizon}---the transition point where syntactic/structural
encoding gives way to factual grounding in retrieved context.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E4_full_33layer_drift_case_studies.png}
  \caption{Full 33-layer L2 drift trajectories from detailed case studies.
           Gray: average-baseline cases ($\dlogp \approx 0$).
           Red: top-divergence cases ($\dlogp < -2$).
           The Integration Horizon at layers 18--20 is visible in both groups,
           but magnitude differs substantially.}
  \label{fig:layerwise}
\end{figure}

\paragraph{Critical null finding: drift cannot separate factuality categories.}
Using three-checkpoint drift (layers 0, 16, 32) across all sweep ablations,
we compare mean L2 drift at layer 32 by taxonomy category
(Table~\ref{tab:layer_drift}). \emph{Factuality-critical} documents (mean
L32\,=\,15.68) produce \emph{indistinguishable} drift from
\emph{confidence-only} documents (14.05). Both are far above neutral (5.65),
but the two categories that logprob-based attribution cannot separate---the
``truly useful'' vs the ``merely confident'' documents---also cannot be
separated by their representation drift.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
Category & $n$ & Mean $\dltwo^{32}$ & Mean AUC \\
\midrule
Factuality-critical   & 16 & 15.68 & 147.8 \\
Factuality-disrupting &  9 & 16.73 & 156.7 \\
Confidence-only       & 46 & 14.05 & 133.3 \\
\midrule
Factuality-weak       & 17 & 10.03 &  98.9 \\
Fact-only             &  7 &  7.96 &  70.8 \\
Neutral               &378 &  5.65 &  53.6 \\
\bottomrule
\end{tabular}
\caption{Mean L2 drift at layer 32 and area under the drift curve (layers 0--32)
by importance category. Factuality-critical and confidence-only documents produce
nearly identical drift magnitudes, confirming that $\dltwo$ cannot substitute
for $\dfact$ in distinguishing factually useful from merely influential documents.}
\label{tab:layer_drift}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E4_layerwise_drift_by_taxonomy.png}
  \caption{Mean L2 drift at layers 0, 16, and 32 by importance category.
           All logprob-important categories (factuality-critical, confidence-only,
           factuality-disrupting) show sharp late-layer increases while neutral
           documents remain near zero. The similarity between factuality-critical
           and confidence-only trajectories confirms that drift magnitude is
           directionally agnostic.}
  \label{fig:layerwise_cat}
\end{figure}

This finding has an important implication: representation drift---like logprob
degradation---measures the \emph{magnitude} of a document's influence on the
model, not its \emph{direction} or \emph{factual consequence}. Factuality-aware
attribution requires $\dfact$ to provide the factual directionality that
neither $\dlogp$ nor $\dltwo$ can supply alone.

\subsection{RQ5: Is Causal Influence Localized to Factual Tokens?}

Token-level logprob analysis (Figure~\ref{fig:token}) on 10 detailed case
studies reveals that log-probability collapse is concentrated on factual tokens
(named entities, numbers, dates), which comprise 48\% of output tokens by count.
Structural tokens (prepositions, conjunctions, determiners) exhibit minimal
change under document ablation.

\begin{figure}[h]
  \centering
  \includegraphics[width=\linewidth]{results/factuality_aware_extensions_20260430_013805/plots/E3_token_type_logprob_drop_boxplot.png}
  \caption{Per-token logprob change under document ablation, for top-divergence
           (left) and average-baseline (right) cases. Factual tokens (orange)
           show substantially larger negative shifts than structural tokens
           (blue) in top-divergence cases; both are near-zero in average-baseline
           cases. Token types identified via heuristics: digit-containing tokens
           and capitalized tokens with $|t| > 1$.}
  \label{fig:token}
\end{figure}

This token-level sparsity parallels the document-level sparsity: causal
influence in RAG is doubly concentrated---in a small fraction of documents,
and within those documents' effects, in a small fraction of output tokens.
The implication is that fact-token logprob drop may be a tighter predictor
of factual impact than whole-sequence logprob drop, though confirming
this quantitatively requires token-level data at larger scale than the 10
case studies analyzed here (see Section~\ref{sec:limitations}).


%% ============================================================
\section{Discussion}
%% ============================================================

\subsection{The Asymmetry at the Heart of Attribution Failure}

The central finding of this paper is a structural asymmetry: in the mixed
retrieval setting, ground-truth documents are factuality-critical 62.5\% of
the time (when logprob-important), while counter-factual documents are
confidence-only 71.2\% of the time. Both categories produce large logprob
collapse and large representation drift---making them indistinguishable to
attribution methods that rely on $\dlogp$ or $\dltwo$ alone.

This asymmetry explains why logprob-only attribution fails on 3 of 12
non-trivial queries: the counter-factual document's logprob collapse is
larger than the ground-truth document's, causing it to rank first. The
ground-truth document---the one that actually determines factual correctness---is
buried below a confidence-only decoy.

\subsection{The Role of Document Count}

As the number of retrieved documents increases, the influence-usefulness gap
narrows (Table~\ref{tab:sweep}). This is not because factuality-aware methods
become less necessary, but because the logprob-important rate drops sharply
as more documents share the logprob burden. At $n=10$, the single GT document
produces less logprob collapse (it is ``diluted'' by 9 CF documents). This
suggests that retrieval systems with large context windows may partially
alleviate the logprob overestimation problem---not by improving attribution,
but by reducing each document's individual logprob footprint.

\subsection{Implications for Attribution Methods}

We do not claim that likelihood-based attribution is wrong. It correctly
identifies which documents shift the model's output distribution. Our claim is
narrower: likelihood shift is an incomplete criterion for identifying
\emph{factuality-impacting} documents. Attribution methods that rank documents
by $|\dlogp|$ should be extended with a factuality filter---either through
post-hoc correction (as in our factuality-aware score, Eq.~\ref{eq:fa_score})
or by modifying the attribution objective to weight likelihood changes by
factual alignment.

Critically, representation drift does not provide the missing signal:
factuality-critical and confidence-only documents are drift-equivalent at
layer 32. The factual directionality of a document's influence is not
encoded in the magnitude of its representational footprint.

\subsection{Implications for RAG Evaluation}

Our results suggest that RAG evaluation metrics should track not just aggregate
accuracy but also the proportion of causally important documents that are
factuality-impacting. A RAG system with low CDR (most influential documents
actually affect factuality) is more reliable than one with high CDR (most
influential documents are confidence-only noise). The \lodo framework can serve
as a diagnostic tool for auditing retrieval corpus quality and retriever behavior,
and the mixed retrieval protocol we introduce provides a controlled benchmark for
measuring attribution quality in adversarial retrieval conditions.

\subsection{Future Directions}

The most immediate extension is factuality-aware reranking: use \lodo-derived
labels to fine-tune the retriever or reranker toward selecting documents that
are factuality-impacting rather than merely confidence-shifting. The integration
horizon finding (Section 6.4) suggests that mid-layer representations---specifically
around layers 18--20---could serve as a real-time signal to flag generation
episodes where retrieved context is having strong but factually irrelevant
effects, potentially before decoding completes.

A direct application of the factuality-aware score (Eq.~\ref{eq:fa_score}) is
to use LODO labels as supervision for training a lightweight attribution
head on top of a frozen model. We leave this to future work.


%% ============================================================
\section{Limitations}
\label{sec:limitations}
%% ============================================================

\paragraph{Post-hoc and computationally expensive.}
\lodo requires $n$ additional forward passes per query (one per ablated
document), plus factuality evaluation of each generated answer.
At inference time, factuality labels are unavailable before generation, so
\lodo cannot be used for online retrieval decisions without an oracle. It is
best suited as a diagnostic and evaluation tool.

\paragraph{Factuality evaluator quality.}
The $\dfact$ signal depends on the quality of the factuality scorer $f$.
We use exact-match and keyword-match evaluation from the RGB benchmark,
which may underestimate factual impact for semantically equivalent
paraphrases. A stronger entailment-based evaluator could increase the
number of observed factuality-impacting cases.

\paragraph{Mixed-document complexity.}
The mixed retrieval setting introduces complex document interactions: in some
cases, removing a counter-factual document changes the model's response even
when the ground-truth document is present. These interactions make it harder
to cleanly attribute factual correctness to a single document. A fully
controlled position-shuffled ablation would sharpen the analysis.

\paragraph{Ranking evaluation scale.}
The factuality-aware ranking comparison (Table~\ref{tab:ranking}) is based on
12 non-trivial queries. While larger than previous evaluations of this type,
the sample is still small enough that per-query variation is significant.
Scaling to more queries and more diverse retrieval scenarios is an important
next step.

\paragraph{Token-level analysis is limited.}
The fact-token logprob analysis (Section 6.5) is based on 10 case studies.
With only 10 observations, we cannot quantify whether fact-token logprob
predicts factual impact better than whole-sequence logprob. Scaling this
analysis to all ablations requires re-running inference with token-level output
recording.

\paragraph{Positional encoding confound.}
Standard \lodo removes document $d_i$ and shifts all subsequent document
positions, changing their RoPE encodings. Position-controlled ablation
(replacing removed documents with length-matched neutral placeholders) has
been implemented but not yet run at scale. The Integration Horizon at layers
18--20 may partially reflect positional re-encoding rather than semantic
absence.

\paragraph{Single model and dataset.}
All results use Llama-3.1-8B-Instruct on a single benchmark. Generalizability
to other model families, scales, and retrieval settings requires broader
evaluation.


%% ============================================================
\section{Conclusion}
%% ============================================================

We have argued and empirically demonstrated that causal influence on output
probability is an incomplete criterion for document importance in RAG.
In a controlled mixed retrieval setting---one ground-truth document among
$(n-1)$ counter-factual adversaries---the overall Collapse Divergence Rate is
63.2\%: nearly two-thirds of logprob-important documents cause zero factual
change. But the most revealing finding is the asymmetry by document type:
ground-truth documents are factuality-critical 62.5\% of the time, while
counter-factual documents are confidence-only 71.2\% of the time. Both types
produce comparable log-probability collapse and comparable representation drift,
making them indistinguishable to existing attribution methods.

We proposed Factuality-Aware \lodo and a six-category importance taxonomy that
reveals this asymmetry. Factuality-aware scoring achieves P@1\,=\,1.00 and
MRR\,=\,1.00 across 12 non-trivial queries, versus 0.73 and 0.83 for
logprob-only. Mechanistically, the Integration Horizon at layers 18--20 is
present for both factuality-critical and confidence-only documents, and their
layer-32 drift magnitudes are indistinguishable---confirming that $\dfact$ is
the irreducible axis for factuality-aware attribution.

Together, these findings motivate a principled extension of RAG attribution:
not abandoning influence-based methods, but supplementing them with factuality
alignment to distinguish documents that genuinely affect model factuality from
those that merely alter its confidence.


\bibliography{references}
\bibliographystyle{acl_natbib}

\end{document}
